#include "utilities.h"
#include<iostream>

using namespace std;

int main(int argc, char** argv){
	if (argc >3){
		char * in = argv[1];
		char * out = argv[2];
		char * vocab = argv[3];

		translateFile(vocab, in, out);
	} else {
		cout << "Not enough input files" <<endl;
	}

	return 0;

}
